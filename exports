# Preferred editor for local and remote sessions
export EDITOR='nvim'

# don't update non-relevant dependencies when installing a package
export HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK=1

# Path to your oh-my-zsh installation.
export ZSH=$HOME/.oh-my-zsh

# Add ~/bin to PATH
export PATH=$PATH:~/bin
export PATH=/usr/local/bin:$PATH
export PATH="$HOME/bin:$PATH"
export PATH=$PATH:~/.local/bin

# Set CLICOLOR if you want Ansi Colors in iTerm2 (OS-X specific)
export CLICOLOR=1
export EXTERNAL_LIBCLANG_PATH="/usr/local/Cellar/llvm/6.0.0/lib/libclang.dylib"
export PKG_CONFIG_PATH=/usr/local/lib/pkgconfig
export PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:/usr/local/lib
export LIBRARY_PATH=$LIBRARY_PATH:/usr/local/opt/openssl/lib/

# Poetry (python package manager)
export PATH="$HOME/.poetry/bin:$PATH"
# add a python3 lib to $PATH to use tmux's powerline feature
export PATH="$HOME/.pyenv/versions/3.7.4/bin/python:$PATH"

# used for gh CLI
export HOMEBREW_GITHUB_API_TOKEN=$(gh auth token)
